# 不初限时狂欢商城 · 后台业务规则（脚本锁死）

> 以下规则为后台运营核心约束，已写入代码与操作脚本，不得擅自变更。

---

## 1. 自动合并规则

### 1.1 合并触发条件

「待发货」列表中，**只有当以下 4 项全部完全一致**时，才允许被系统判定为同一客户并自动合并：

1. 姓名
2. 电话
3. 地址
4. 微信号

### 1.2 判定说明

- 4 项中**任意一项不同**，均视为不同客户，**不合并**。
- 微信号为空字符串时，按空值参与比较；两个空微信号订单在其余 3 项一致时才合并。
- 合并仅用于展示与发货操作，**不修改底层订单数据**；每笔订单仍保留独立订单号、付款时间、商品明细与金额。

### 1.3 代码落点

- `public/admin.html`：`renderMergedShip()` 函数中以 `${name}|${phone}|${address}|${wechat}` 作为分组 key。
- 每次加载「待发货」列表时动态计算，不持久化合并状态。

---

## 2. 金额核对机制

### 2.1 核心原则

凡涉及客户金额的环节，必须**先显示金额明细、经人工核对确认后再执行**。

### 2.2 必须核对的环节

1. **确认收款（待确认 → 待发货）**
   - 弹窗展示：客户姓名、订单号、商品明细、数量、本单合计金额。
   - 操作者点击「确认已收款」后，才允许调用 `/api/orders/:id/confirm`。

2. **单个订单发货（待发货 / 已发货）**
   - 填写快递单号前，弹窗展示：客户姓名、商品明细、数量、合计金额。
   - 操作者核对金额无误后，再进入快递单号输入流程。

3. **合并订单发货（待发货）**
   - 填写快递单号前，弹窗展示：客户姓名、合并订单笔数、每笔子订单的商品明细与金额、合并合计金额。
   - 操作者核对总金额无误后，才允许填写单号并一次性应用到该客户全部订单。

### 2.3 金额显示格式

- 金额统一使用 `¥` 符号，保留两位小数。
- 合计金额必须与本单/本组所有商品 `price × qty` 之和一致。

### 2.4 代码落点

- `public/admin.html`：`confirmPaid(id)`、`ship(id)`、`shipGroup(ids)` 函数。
- 服务端 `server.js`：`/api/orders/:id/confirm` 与 `/api/orders/:id/ship` 端点只做状态/单号更新，不修改金额；金额由前端根据原始订单数据计算并展示。

---

## 3. 发货文本格式规则

### 3.1 合并发货文本

点击「生成发货文本」后，复制到剪贴板的内容格式如下：

```
<姓名> <电话>
<地址>

<商品名> ×<数量>
<商品名> ×<数量>
...
```

### 3.2 格式约束

- 不显示「姓名」「电话」「地址」等字段名。
- 不显示订单号。
- 不显示付款时间。
- 不显示金额。
- 仅输出：客户联系信息 + 所有商品及对应数量。

### 3.3 组合/链接产品的明细展开

- 若订单中的产品设置了 `bundleItems`（后台产品编辑页「该价格链接包含的产品明细」），点击「生成发货文本」时按明细展开。
- 展开格式示例（购买 1 份）：
  ```
  经典面膜 · 5盒线雕囤货装
  • 线雕面膜   5 盒
  • 液   10 瓶
  ```
- 若客户购买 `n` 份，在产品名后加 `*n`，每条明细后加 `*n`：
  ```
  经典面膜 · 5盒线雕囤货装 *2
  • 线雕面膜   5 盒   *2
  • 液   10 瓶   *2
  ```
- 未设置 `bundleItems` 的产品仍按原格式 `${name} ×${qty}` 输出。

### 3.4 代码落点

- `public/admin.html`：`copyShipGroup(ids)` 函数。
- `public/admin.html`：产品编辑弹窗 `pBundle` 文本框与 `parseBundleText()`。
- `server.js`：`/api/products` 保存接口持久化 `bundleItems`。

---

## 4. 修订记录

| 时间 | 修订人 | 内容 |
|------|--------|------|
| 2026-08-25 | WorkBuddy | 初始版本，锁死合并规则、金额核对、发货文本格式 |
