# 交接 · 不初限时狂欢商城（继续工作用）

> 写于 2026-08-25 18:15，由上一会话 WorkBuddy 交接。新账号直接读这份文件即可无缝继续，无需柒木重复交代背景。

## 一、主线任务

**不初限时狂欢商城**（清仓商城）——柒木要微信可分享的私域电商小程序式网页商城，顾客在微信里打开、下单、选支付方式、付款后截图给夏天老师（柒木本人），商家在后台确认收款并发货。

- 项目根目录：`C:\Users\华为mate14\WorkBuddy\2026-08-24-15-09-30\clearance-shop\`
- 服务地址：**http://localhost:4100**
- 客户页（首页）：`http://localhost:4100/`
- 商家后台：`http://localhost:4100/admin`
- 订单示例页（待付款）：`http://localhost:4100/order/5EBE92AA`

## 二、技术栈（已验证可用）

- **纯 Node.js 内置模块** HTTP 服务器（`server.js`），零依赖、无需 npm install。
- Node 路径：`C:\Users\华为mate14\.workbuddy\binaries\node\versions\22.22.2\node.exe`
- 前端：原生 HTML/CSS/JS，服务端模板渲染（`{{占位符}}` + `renderTemplate` / `jsonForScript`）。
- 启动：`launcher/启动不初限时狂欢商城.cmd`（GBK 编码、自动杀旧进程、打开浏览器）。
- 静默启动器：`launcher/启动不初限时狂欢商城.vbs`（隐藏命令行窗口）。
- 桌面快捷方式：`C:\Users\华为mate14\Desktop\不初限时狂欢商城.lnk`（客户商城）、`不初限时狂欢商城后台.lnk`（商家后台），均为 .lnk 并显示新图标。
- 图片处理：PIL（分享卡片 JPG）、cv2 GrabCut（银行卡抠图，`tools/extract_card.py`）。

## 三、目录结构

```
clearance-shop/
├── server.js              # 主服务（全部 API + 路由 + 模板渲染）
├── package.json
├── Dockerfile / deploy/   # 云端部署配置（fly.toml / render.yaml / 部署指南.md）
├── launcher/启动不初限时狂欢商城.cmd
├── launcher/启动不初限时狂欢商城.vbs
├── launcher/打开不初限时狂欢商城后台.vbs
├── launcher/不初限时狂欢商城.ico
├── README.md
├── payment-preview.html   # 支付方式静态预览页（开发用，可删）
├── data/
│   ├── config.json        # 商城配置（店名、联系人、支付二维码、银行卡、公告、分享图）
│   ├── products.json      # 商品列表（5 个商品）
│   └── orders.json        # 订单数据（11 个订单，原子写入+备份）
├── views/
│   ├── home.html          # 首页（轮播公告、商品列表、购物车抽屉、"查询我的订单"弹窗、"收藏此商城"）
│   ├── product.html       # 商品详情页
│   └── order.html         # 订单页（支付方式 tab、催单便签、物流跟踪、进度条）
├── public/
│   ├── admin.html         # 商家后台（订单管理 + 商品管理 + 图库管理）
│   └── assets/
│       ├── payment-wechat.jpg   # 微信收款码
│       ├── payment-alipay.jpg   # 支付宝收款码
│       ├── bank-card.png        # 银行卡抠图（透明背景 PNG）
│       ├── bank-card.jpg        # 银行卡原图
│       ├── share.jpg            # 微信分享大图
│       ├── gallery/默认图库/     # 后台图库（当前为空）
│       └── products/            # 商品图（SVG 占位 + *-share.jpg 分享图）
└── tools/
    ├── gen_share.py       # PIL 生成分享 JPG
    └── extract_card.py    # cv2 GrabCut 抠银行卡
```

## 四、核心数据（当前状态）

### config.json（2026-08-25 更新）
```json
{
  "shopName": "不初限时狂欢商城",
  "contactName": "夏天老师",
  "paymentQr": "/assets/payment-qr.svg",
  "paymentWechatQr": "/assets/payment-wechat.jpg",
  "paymentAlipayQr": "/assets/payment-alipay.jpg",
  "bankImage": "/assets/bank-card.png",
  "bankName": "工商银行（万里支行）",
  "bankHolder": "席娟",
  "bankAccount": "622208 1106002836 790",
  "announcement": "因全家迁居云南，仓库关仓，不初品牌产品全部超低价回馈老客户。卖完即止，未来一年难再得。",
  "shareImage": "/assets/share.jpg"
}
```

### products.json（5 个商品，图片是 SVG 占位，真实图待柒木后台传）
| id | 名称 | 价格 |
|---|---|---|
| blind-299 | 告别盲盒 · 299档 | 299 |
| blind-599 | 告别盲盒 · 599档 | 599 |
| blind-999 | 告别盲盒 · 999档 | 999 |
| essence-10 | 经典精华 · 10瓶装囤货装 | 299 |
| essence-5 | 经典精华 · 5瓶装囤货装 | 399 |

### orders.json（11 个订单）
- **待付款（3）**：875242C9、5EBE92AA、109A5950 —— ⚠️ 按最新规则，待付款订单**不会**出现在商家后台"待处理"tab
- **已发货（7）**：AC4400E1、855C957B、969306E4、5D2D8CB5、30F784B3、8A0AF2EA、6374A443
- **待发货（1）**：1F885ADA
- 订单字段：`id, items[{id,name,price,qty}], total, name, phone, address, note, status, tracking, createdAt, confirmedAt, shippedAt, paidScreenshotAt`

## 五、API 清单（server.js）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/products | 商品列表 |
| GET | /api/config | 商城配置 |
| GET | /api/orders | 全部订单（后台用） |
| GET | /api/orders/lookup?key= | 姓名/手机号查订单（多单） |
| POST | /api/orders | 创建订单 |
| POST | /api/orders/:id/paid | 客户点"已付款"→ status 待付款→待确认（记录 paidScreenshotAt） |
| POST | /api/orders/:id/confirm | 商家确认收款 → 待确认→待发货（记录 confirmedAt） |
| POST | /api/orders/:id/ship | 商家发货填单号 → 待发货→已发货（记录 shippedAt） |
| GET | /api/gallery | 图库列表 |
| POST | /api/gallery/folder | 建图库文件夹 |
| POST | /api/gallery/upload | 图库上传图片 |
| POST | /api/products | 新增/更新商品 |
| POST | /api/products/reorder | 商品拖拽排序 |
| DELETE | /api/products/:id | 删除商品 |
| GET | / | 首页（渲染 home.html） |
| GET | /product/:id | 商品详情 |
| GET | /order/:id | 订单页 |
| GET | /admin | 商家后台 |
| GET | /assets/* | 静态资源 |

**订单状态流转**：待付款 →(客户点"已付款，并将付款页面发给夏天老师本人完毕")→ 待确认 →(商家后台"确认收款")→ 待发货 →(商家发货填单号)→ 已发货

## 六、本次会话完成的工作（2026-08-25）

1. **真实支付图片接入**：微信收款码→`payment-wechat.jpg`、支付宝收款码→`payment-alipay.jpg`、银行卡正面→`bank-card.jpg`，config.json 增加 `paymentWechatQr / paymentAlipayQr / bankImage / bankName / bankHolder / bankAccount`。
2. **银行卡 tab 三层布局**：上层银行卡图片（宽 100% 充满）、中层卡号 +「复制」按钮、下层收款人姓名/开户行 +「复制全部银行卡信息」。
3. **银行卡抠图**：cv2 GrabCut 抠出透明背景 `bank-card.png`（config 已指向 png），减少占面积、更正规。
4. **付款区客服提示**：订单页付款 tip 下加"💬 如有付款疑问，可联系夏天老师本人 微信：buchu2018 / buchu2021"。
5. **Bug 修复（重要）**：商家后台"待处理"tab 只显示 `status==='待确认'`（客户已点已付款按钮）的订单；刚提交未付款的订单不再提前出现在后台，避免误点"确认收款"。

## 七、待办 / 未完成事项（新账号优先处理）

1. **真实商品图片**：当前商品图是 SVG 占位（`public/assets/products/*.svg`）。柒木决定自己通过后台 `/admin` 的"商品管理"上传真实图（图库已建好"默认图库"文件夹）。**提醒柒木去上传**，或问她要图片由 AI 直接放入。
2. **真实收款二维码确认**：微信/支付宝收款码图片已就位，但需柒木确认这确实是她要展示的收款码（若是"收款码"而非"付款码"页面对应关系需复核）。
3. **云端部署（微信卡片生效的前提）**：`deploy/` 里已备好 Fly.io / Render 配置和 `部署指南.md`，但**尚未部署**。微信内分享卡片（og:image/og:url 绝对地址）只有部署到公网域名才生效。柒木要用手机微信真机测试分享卡片 → 必须部署。
4. **店名统一**：已统一为"不初限时狂欢商城"，客服名"夏天老师"保持不变。
5. **旧订单清理**：orders.json 里 11 个订单多为测试单（翟/王子/夏天等），正式上线前需柒木确认哪些删哪些留。

## 八、环境信息 & 踩坑记录（重要）

- **服务器运行方式**：WorkBuddy 后台任务 `node server.js`，端口 4100（当前 PID 22904）。重启流程：`taskkill /F /PID <pid>` → `cd 项目目录 && node server.js`。
- ⚠️ **WorkBuddy 沙箱限制**：Bash 工具默认沙箱可能写不进工作区外文件；改项目内文件没问题。启动后台任务用 `run_in_background: true`。
- ⚠️ **.cmd 必须 GBK 编码**（chcp 936），UTF-8 会乱码（已踩坑修复）。
- ⚠️ **订单页曾有白屏 bug**：order.html 里多了一个 `}` 导致 JS 语法错误——改 JS 后务必先 `node --check` 或浏览器控制台验证。
- ⚠️ **数据写入有写锁 + 原子写**（writeJsonAtomic + withLock），改数据文件不要绕过。
- ⚠️ **rembg 在这台机器装不上**（onnxruntime 编译失败），抠图改用 `cv2 GrabCut`（`tools/extract_card.py`，已可用）。不要再浪费时间装 rembg。
- Python 路径：`C:\Users\华为mate14\.workbuddy\binaries\python\versions\3.13.12\python.exe`（PIL、cv2、numpy 已装）。
- 桌面图标已合并为两个 .lnk：`不初限时狂欢商城.lnk`（商城，启动服务+打开浏览器）、`不初限时狂欢商城后台.lnk`（打开 /admin）。旧的柒木商城快捷方式已移入 `launcher/旧桌面图标备份`。

## 九、给新账号的开场建议

柒木的指令通常很短（如"继续"、"看下这个 bug"）。开场可主动汇报：

> 已读取交接文件。商城运行在 http://localhost:4100（客户首页 /admin 后台）。当前待办：① 商品真实图上传 ② 云端部署（微信分享卡片依赖）③ 旧测试订单清理 ④ 确认图标/店名是否满意。这次要继续做什么？

然后等柒木指令。涉及修改 JS 后必须重启服务验证；涉及 config 改动直接改 `data/config.json`（server.js 有默认兜底）。
