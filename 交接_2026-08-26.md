# 交接 · 不初限时狂欢商城（2026-08-26 14:05）

> 本文件由 WorkBuddy 会话编写，供**新 WorkBuddy 账号**直接读取、无缝继续工作。新账号读完即可上手，无需柒木重复交代背景。

---

## 一、项目概览

**不初限时狂欢商城**（清仓商城）——柒木的私域电商网页商城。顾客在微信里打开、浏览商品、下单、选支付方式（微信/支付宝/银行卡）、付款后截图给夏天老师（柒木本人），商家在后台确认收款并发货。

| 项目 | 值 |
|------|-----|
| 项目根目录 | `C:\Users\华为mate14\WorkBuddy\2026-08-24-15-09-30\clearance-shop\` |
| 本地服务地址 | **http://localhost:4100** |
| 买家首页 | `http://localhost:4100/` |
| 商家后台 | `http://localhost:4100/admin` |
| 云端公网地址 | **https://buchu-shop.onrender.com** |
| 云端后台 | `https://buchu-shop.onrender.com/admin` |
| GitHub 仓库 | `https://github.com/YX8686/clearance-shop` |
| 数据后端 | Supabase（Singapore 节点，免费层） |

### 架构一句话
`代码放 GitHub` → `Render 拉代码跑 Node 服务` → `订单/产品/配置/图片存到 Supabase` → `本地后台也连 Supabase`（双模式：本地双击图标自动读 `.env.local` 切云端）

---

## 二、当前运行状态

- 本地 Node 服务正在运行：**PID 20692**，端口 **4100**，模式 = **Supabase 云端**
- 云端 Render 服务：`https://buchu-shop.onrender.com`（Free 计划，Singapore 节点）
- 保活脚本：Windows 启动文件夹 `buchu-shop-keepalive.vbs`（每 5 分钟 curl ping）
- Node 路径：`C:\Users\华为mate14\.workbuddy\binaries\node\versions\22.22.2\node.exe`
- Python 路径：`C:\Users\华为mate14\.workbuddy\binaries\python\versions\3.13.12\python.exe`

---

## 三、云端凭据（已脱敏，勿外泄）

| 凭据 | 值 | 存放位置 |
|------|-----|---------|
| Supabase URL | `https://pzhblcoszvkbnmolkkxf.supabase.co` | `.env.local` + Render 环境变量 |
| Supabase Publishable Key | `sb_publishable_AT0jjmkKEHk3PJThm9aG1w_q2hSfV4y` | `.env.local` + Render 环境变量 |
| GitHub PAT | （本地 git remote 内嵌，勿明文入库） | git remote URL |
| Render API Key | `rnd_...`（用户会话内，未写入文件） | — |
| Render Service ID | `srv-da74779srm7s73fb8uvg` | — |
| Render Owner ID | `tea-da6r5afavr4c739pbqag` | — |

> `.env.local` 已加入 `.gitignore`，不会推到 GitHub。

---

## 四、云端数据现状（2026-08-26 14:00 实查）

### 产品（6 个）

| ID | 名称 | 价格 | 主图 |
|----|------|------|------|
| blind-299 | 告别盲盒 · 299档 | 299 | `/assets/products/blind-299.svg`（SVG 占位） |
| blind-599 | 告别盲盒 · 599档 | 599 | `/assets/products/blind-599.svg`（SVG 占位） |
| blind-999 | 告别盲盒 · 999档 | 999 | `/assets/products/blind-999.svg`（SVG 占位） |
| essence-10 | 经典精华 · 10瓶装囤货装 | 299 | `/assets/products/essence-10.svg`（SVG 占位） |
| essence-5 | 经典精华 · 5瓶装囤货装 | 399 | `/assets/products/essence-5.svg`（SVG 占位） |
| E1589F05 | 经典面膜 · 5盒线雕囤货装 | 599 | `/assets/gallery/默认图库/单品 线雕面膜5盒+10瓶液_mt8nyo0e.png`（**1MB PNG**） |

### 订单（5 条）

| ID | 客户 | 状态 | 金额 | 下单时间 |
|----|------|------|------|---------|
| 6A6A4AFE | 小米 | 已发货 | ¥898 | 08-26 10:04 |
| C1EC5E64 | 小米 | 已发货 | ¥1,497 | 08-26 10:36 |
| 774972AA | 小王 | 已发货 | ¥599 | 08-26 11:33 |
| 8FE2D2FE | 小王 | 待发货 | ¥1,298 | 08-26 13:08 |
| 2E52233E | 小米 | 已发货 | ¥999 | 08-26 13:57 |

状态分布：已发货 4 / 待发货 1。全部为真实测试订单（小米=柒木本人，小王=朋友）。

---

## 五、技术栈

- **服务端**：纯 Node.js 内置模块 HTTP 服务器（`server.js`），零依赖核心 + `@supabase/supabase-js`
- **前端**：原生 HTML/CSS/JS，服务端模板渲染（`{{占位符}}` + `renderTemplate` / `jsonForScript`）
- **数据层**：Supabase `shop_data` 表（KV 模式，key=products/config/orders）+ Supabase Storage（shop 桶，存图片）
- **部署**：GitHub → Render（自动 HTTPS、免备案）
- **保活**：Windows 启动文件夹 VBS 脚本，每 5 分钟 curl ping

### 目录结构
```
clearance-shop/
├── server.js              # 主服务（全部 API + 路由 + 模板渲染，588 行）
├── .env.local             # 本地启动连云端的凭据（gitignored）
├── package.json           # 依赖仅 @supabase/supabase-js
├── render.yaml            # Render 部署配置
├── DEPLOY.md              # 完整云端部署教程
├── business_rules.md      # 业务规则文档
├── launcher/
│   ├── 启动不初限时狂欢商城.cmd    # 双击启动服务+开浏览器
│   ├── 启动不初限时狂欢商城.vbs    # 静默启动（隐藏 cmd 窗口）
│   ├── 打开不初限时狂欢商城后台.cmd
│   └── 不初限时狂欢商城.ico
├── data/                  # 本地种子数据（云端模式下仅作 fallback）
│   ├── products.json
│   ├── config.json
│   └── orders.json
├── views/
│   ├── home.html          # 买家首页（公告轮播、商品列表、购物车、订单恢复弹窗）
│   ├── product.html       # 商品详情页
│   └── order.html         # 订单页（付款 tab、催单便签、物流跟踪、进度条）
├── public/
│   ├── admin.html         # 商家后台（订单管理 + 商品管理 + 图库管理，~42KB）
│   ├── business_rules.js
│   └── assets/
│       ├── payment-qr.svg       # 默认收款码
│       ├── payment-wechat.jpg   # 微信收款码
│       ├── payment-alipay.jpg   # 支付宝收款码
│       ├── bank-card.png        # 银行卡抠图
│       ├── share-square.jpg     # 微信分享大图
│       ├── products/            # 商品图（SVG 占位 + *-share.jpg 分享图）
│       └── gallery/默认图库/     # 图库（含用户上传的大图）
├── tools/
│   ├── gen_share.py       # PIL 生成分享 JPG
│   └── extract_card.py    # cv2 GrabCut 抠银行卡
└── deploy/                # 云端部署辅助文件
```

---

## 六、API 清单（server.js）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/products | 商品列表（从内存返回，boot 时已 loadKV） |
| GET | /api/config | 商城配置（从内存返回） |
| GET | /api/orders | 全部订单（**每次从 Supabase 实时读取**） |
| GET | /api/orders/lookup?key= | 姓名/手机号查订单 |
| GET | /api/orders/:id | 单个订单（供前端恢复「待付款」订单） |
| POST | /api/orders | 创建订单（先 syncOrders 刷内存，再 push，再 saveOrders） |
| POST | /api/orders/:id/paid | 客户点"已付款" → 待付款→待确认 |
| POST | /api/orders/:id/confirm | 商家确认收款 → 待确认→待发货 |
| POST | /api/orders/:id/ship | 商家发货填单号 → 待发货→已发货 |
| POST | /api/config | 更新配置（店名/联系人/公告/收款码） |
| GET | /api/gallery | 图库列表（本地文件系统） |
| POST | /api/gallery/folder | 建图库文件夹 |
| POST | /api/gallery/upload | 图库上传图片（Supabase 模式上传到 Storage） |
| POST | /api/products | 新增/更新商品 |
| POST | /api/products/reorder | 商品拖拽排序 |
| DELETE | /api/products/:id | 删除商品 |
| GET | / | 首页（渲染 home.html） |
| GET | /product/:id | 商品详情 |
| GET | /order/:id | 订单页 |
| GET | /admin | 商家后台 |
| GET | /assets/* | 静态资源 |

**订单状态流转**：待付款 →(客户点"已付款并截图")→ 待确认 →(商家后台"确认收款")→ 待发货 →(商家发货填单号)→ 已发货

---

## 七、已完成的修复（本会话及之前）

### 2026-08-26 13:43 关键 bug 修复：确认收款静默失败

**根因**：`server.js` 的 `saveKV()` 在 Supabase `upsert` 失败时只 `console.error` 静默吞掉，不抛错 → API 返回 `{ok:true}` 但云端从未写入 → 重启后状态回退。

**已修复**：
1. Supabase client 注入 30s `AbortController` 超时（不再无限挂起）
2. `saveKV` 失败 `throw new Error`（不再静默）→ API 返 500 JSON
3. `loadKV` 失败时标 `failed:true`，下次立即重试不读陈旧值
4. `boot()` 加 try/catch 优雅降级到本地种子
5. `admin.html` 的 confirmPaid / ship / shipGroup / saveSettings 加 5xx 检测 + `window.confirm` 询问重试
6. `load()` GET 失败显示「⚠️ 数据加载失败」+「重新加载」按钮

### 其他已完成项
- ✅ 云端部署（Render + Supabase 全自动上线，公网可访问）
- ✅ 本地后台连接云端 Supabase（双击图标即连云端，无需手动设环境变量）
- ✅ 商家后台显示付款时间/下单时间
- ✅ 发货后手机端自动刷新状态
- ✅ 收款码步骤文案优化（保存→微信扫一扫→截图发给夏天）
- ✅ 待付款恢复弹窗（localStorage 存 pendingOrders，跳回商城自动恢复）
- ✅ 待付款弹窗显示商品明细
- ✅ 购物车双按钮（返回继续加购 + 去结算）
- ✅ 收款码 lightbox（长按识别/保存）
- ✅ 产品分享 OG 大图（`*-share.jpg`，1200×630）
- ✅ 后台首屏拆分（先拉 orders 立即渲染，其余异步补齐）
- ✅ 3 秒 TTL 缓存 + 并发去重（减少 Supabase 调用）
- ✅ 先 listen 再 boot（防 Render 健康检查超时）
- ✅ 后台图片上传前端压缩（宽度 1200、JPEG 0.85、<150KB 不压）
- ✅ 保活脚本（VBS 每 5 分钟 ping）

---

## ⚠️ 八、未解决的问题（新账号优先处理）

### 问题 1：商家后台操作非常慢

**症状**：点「确认收款」「发货」等按钮后，页面要等很久（几秒到几十秒）才流转到下一个状态界面。

**根因分析**：每次按钮点击触发 **3 次 Supabase 网络往返**：

| 步骤 | 操作 | Supabase 调用 | 耗时（暖） | 耗时（冷启动） |
|------|------|-------------|-----------|--------------|
| 1 | `syncOrders()` | GET shop_data where key=orders | 0.4-1.4s | **27s** |
| 2 | `saveOrders()` | POST shop_data upsert | 0.4-1.4s | **27s** |
| 3 | 前端 `load()` | GET /api/orders → 又读 Supabase | 0.4-1.4s（可能命中 3s TTL 缓存） | **27s** |

Supabase 免费层（Singapore 节点）实测延迟：冷启动 27s，暖 0.4-1.4s。3 次往返 = 暖 1.2-4.2s，冷启动 80s+。

**当前缓解措施（已做但不够）**：
- 3 秒 TTL 缓存（减少读次数，但写仍需往返）
- 30s 超时（防无限挂起，但超时后用户要手动重试）
- 前端 5xx 检测 + 重试询问

**建议修复方向（按优先级）**：

1. **乐观 UI（最有效，改动最小）**：
   - 前端点击按钮后**立即更新本地 ORDERS 数组 + 重新渲染**，不等服务器返回
   - 后台 POST 返回后如果成功，确认状态；如果失败，回滚 + 提示
   - 这样用户**零等待**，后台同步在后台跑
   - 改动文件：`admin.html` 的 `confirmPaid` / `ship` / `shipGroup`

2. **POST 返回更新后的订单数据**：
   - 当前 POST /confirm 返回 `{ok:true, status}`，前端还要 `load()` 全量重拉
   - 改为返回完整订单对象，前端直接更新本地数组，省掉第 3 次往返
   - 改动文件：`server.js` 的 confirm/ship 路由 + `admin.html`

3. **合并 syncOrders + saveOrders 为一次操作**：
   - 当前先 `syncOrders()`（读）再 `saveOrders()`（写），两次往返
   - 改为：直接从云端读 → 修改 → 写回，一次读一次写
   - 或更激进：本地内存改 + 异步写入（写失败再回滚）

4. **Supabase 区域优化**：
   - 当前 Singapore 节点，国内访问延迟高
   - 可考虑迁移到 Northeast Asia (Tokyo) 或 Southeast Asia (Singapore) 的其他区域
   - 但免费层区域有限，需实测

5. **本地缓存 + 定时同步**：
   - 后台首次加载后把 orders 缓存在 localStorage
   - 每次操作先改本地，定时同步到云端
   - 但会增加数据不一致风险

### 问题 2：买家商城图片加载极慢

**症状**：买家在手机端打开商城，产品主图（尤其是用户自己上传的图片）显示非常慢，几乎不能完全显示。

**根因分析**：

| 图片来源 | 大小 | 问题 |
|---------|------|------|
| SVG 占位图（blind-299 等 5 个原生商品） | ~500 bytes | 无问题，秒加载 |
| 用户上传图 `单品 线雕面膜5盒+10瓶液_mt8nyo0e.png` | **1,066,968 bytes (1MB)** | 手机下载 1MB PNG 极慢 |
| 用户上传图 `8_mt8pmgar.png`（详情图） | **2,050,287 bytes (2MB)** | 手机下载 2MB PNG 极慢 |

核心问题：
1. **图库上传不压缩**：`admin.html` 的产品主图/详情图上传有前端压缩（宽 1200px、JPEG 0.85），但**图库上传（`/api/gallery/upload`）不做任何压缩**，原图直接存储
2. **PNG 格式**：用户上传的是 PNG（未压缩格式），比 JPEG 大 5-10 倍
3. **图片未上传到 Supabase Storage**：产品 E1589F05 的图片路径是 `/assets/gallery/默认图库/xxx.png`（本地路径），说明是在**本地文件模式**下上传的（`.env.local` 启用前）。图片文件在本地 `public/assets/gallery/` 有，但在 Render 云端不存在 → 云端 404
4. **Supabase Storage 为空**：实查 `shop` 桶，根目录和子目录均无文件 → 说明所有图片上传到 Storage 都失败了（可能 Storage 策略/桶配置问题），saveImage 返回空串，但代码可能走了本地 fallback

**建议修复方向（按优先级）**：

1. **压缩已有大图（立即生效）**：
   - 用 Python PIL 把 `public/assets/gallery/默认图库/` 下的 PNG 转成 JPEG（宽 1200px、质量 85）
   - 1MB PNG → ~100KB JPEG，2MB PNG → ~200KB JPEG
   - 更新 Supabase 云端 products 表里的 image/detailImages 路径指向新文件
   - 推到 GitHub 触发 Render 部署

2. **图库上传加压缩（防未来再发生）**：
   - `admin.html` 的 gallery upload 函数加 Canvas 压缩（和产品图上传一样的逻辑）
   - 强制转 JPEG（除非原图是带透明通道的 PNG）
   - 改动文件：`admin.html`

3. **修复 Supabase Storage 上传**：
   - 检查 Supabase Storage 的 `shop` 桶是否存在且 Public
   - 检查 RLS 策略 `allow_shop_anon` 是否生效
   - 如果 Storage 不可用，图片只能走 Git 仓库（commit 图片文件）或换用其他图床

4. **图片懒加载 + 渐进加载**：
   - `views/home.html` / `product.html` 的 `<img>` 已加 `loading="lazy" decoding="async"`
   - 可加 LQIP（低质量占位图）或 blur-up 效果改善体验
   - 但根本问题还是文件太大

5. **CDN 或国内图床**：
   - Supabase Storage 在 Singapore，国内访问慢
   - 可考虑把图片传到国内 CDN（如七牛/又拍免费层）或 GitHub raw（有 CDN 加速）
   - 但增加复杂度

---

## 九、关键代码位置索引

### server.js（588 行）
| 功能 | 行号 | 关键函数/变量 |
|------|------|-------------|
| `.env.local` 加载 | 17-30 | `loadEnvLocal()` |
| Supabase client 初始化（含 30s 超时） | 32-49 | `sb`, `USE_SUPABASE`, `fetchWithTimeout` |
| KV 读取（3s TTL 缓存 + 失败标记） | 80-115 | `loadKV()`, `kvCache` |
| KV 写入（失败抛错） | 116-126 | `saveKV()` |
| 图片写入（Supabase Storage / 本地） | 130-147 | `saveImage()` |
| boot（优雅降级） | 175-191 | `boot()`, `ensureBoot()` |
| 订单读取（实时读云端） | 216-220 | `getOrders()` |
| 订单写入前同步 | 223-228 | `syncOrders()` |
| 订单状态流转 | 359-387 | `/paid`, `/confirm`, `/ship` 路由 |
| 图库上传 | 435-453 | `/api/gallery/upload` |
| 产品保存 | 456-486 | `/api/products` POST |

### admin.html（~42KB）
| 功能 | 关键函数 |
|------|---------|
| 订单加载 | `load()` — 先拉 orders 立即渲染，其余异步 |
| 确认收款 | `confirmPaid(id)` — 加了 5xx 检测 + 重试询问 |
| 发货 | `ship(id)` — 同上 |
| 批量发货 | `shipGroup(ids)` — 同上 |
| 保存设置 | `saveSettings()` — 同上 |
| 产品保存 | `saveProduct()` — 同上 |
| 图片上传压缩 | 产品主图/详情图上传时有 Canvas 压缩 |
| 图库上传 | gallery upload **无压缩** ← 问题 2 根因之一 |
| 自动轮询 | 15 秒轮询，静默更新 |

### views/home.html（~26KB）
| 功能 | 关键函数 |
|------|---------|
| 购物车 | `openCheckout()`, `submitOrder()` |
| 待付款恢复弹窗 | `checkPendingOrders()`, `pendingModal` |
| 收货信息记忆 | `buyerInfo` localStorage |

### views/order.html（~30KB）
| 功能 | 关键函数 |
|------|---------|
| 付款 tab | 微信/支付宝/银行卡三个 tab |
| 收款码 lightbox | `showQrLightbox()` |
| 催单便签 | `saveQrImage()` |
| 状态自动刷新 | 页面加载后 `fetch /api/orders/:id` 检查状态 |

---

## 十、环境与踩坑记录

### 启动方式
- **本地双击**：桌面 `不初限时狂欢商城.lnk`（商城）或 `不初限时狂欢商城后台.lnk`（后台）
- **命令行**：`cd clearance-shop && node server.js`
- **重启**：`taskkill /F /PID <pid>` → 重新启动
- 端口冲突检查：`netstat -ano | grep :4100`

### 沙箱限制
- WorkBuddy Bash 工具默认沙箱可能写不进工作区外文件
- 写 `AppData/Roaming` 或项目外目录需 `dangerouslyDisableSandbox: true`
- 项目内文件读写无限制

### 代理坑（重要）
- 用户电脑有 Clash（127.0.0.1:7897），fake-ip 模式
- **GitHub API 必须直连**（`--noproxy '*'` 或 `ProxyHandler({})`），走代理会 SSL 断
- **Render API 必须走代理**（`-x http://127.0.0.1:7897`），直连 HTTP 000
- 两者方向相反！

### Supabase 连接坑
- 免费层冷启动慢（27s 首次），暖 0.4-1.4s
- 偶发 `ECONNRESET` / `fetch failed`
- `NODE_TLS_REJECT_UNAUTHORIZED=0`（公司代理环境）会加剧握手失败，但不用又连不上
- 30s 超时已加，但超时后前端要处理重试

### 推送代码到 GitHub
- 本地 `git push` 可能因沙箱 EPERM 被拒
- 备用方案：Python + GitHub Contents API（Bearer token）逐文件 PUT
- 推送后需手动触发 Render 部署（`POST /v1/services/{id}/deploys`）或等自动部署

### .cmd 文件
- 必须 **GBK 编码**（chcp 936），UTF-8 会乱码

### 其他
- `rembg` 在这台机器装不上（onnxruntime 编译失败），抠图用 `cv2 GrabCut`
- 旧交接文件 `交接_新窗口继续.md`（2026-08-25 写的）部分信息已过时，以本文件为准

---

## 十一、可用 Skill（已沉淀的经验）

| Skill | 路径 | 用途 |
|-------|------|------|
| `clearance-shop-order-error` | `~/.workbuddy/skills/clearance-shop-order-error/` | 商城订单错误的排查和修复流程 |
| `node-supabase-render-deploy` | `~/.workbuddy/skills/node-supabase-render-deploy/` | Node + Supabase + Render 部署全流程 |
| `single-file-excel-tool` | `~/.workbuddy/skills/single-file-excel-tool/` | 单文件 HTML 读取 Excel |
| `skill-sync-to-obsidian` | `~/.workbuddy/skills/skill-sync-to-obsidian/` | Skill 同步到 Obsidian 品牌大脑 |

> Skill 主版本在 `~/.workbuddy/skills/`，同步副本在 `D:\Obsidian_品牌大脑_正式库\.workbuddy\skills\`。每次修改 skill 后自动同步。

---

## 十二、给新账号的开场建议

柒木（用户）的指令通常很短（如"继续"、"看下这个 bug"、"图片太慢"）。读完后可主动汇报：

> 已读取交接文件。商城本地运行在 http://localhost:4100（后台 /admin），云端在 https://buchu-shop.onrender.com。当前有 2 个未解决问题：
> 1. 商家后台操作慢（根因：每次按钮 3 次 Supabase 往返，建议做乐观 UI）
> 2. 买家图片加载慢（根因：图库上传不压缩，1-2MB PNG 未转 JPEG）
> 这次要先解决哪个？还是有别的任务？

### 工作流程要点
1. **修改 JS 后必须重启服务**（`taskkill /F /PID <pid>` → `node server.js`）或验证语法（`node --check`）
2. **修改前端 JS 后用 `new (require('vm').Script)(code)` 验证语法**
3. **改动云端代码后**：推 GitHub → 触发 Render 部署 → curl 验证
4. **操作云端数据**：用 supabase-js 直查直改（参考 `clearance-shop-order-error` skill）
5. **涉及用户数据**：先备份，再操作，操作后验证
6. **柒木是白牌护肤品牌「不初」创始人**，夏天老师 = 柒木本人

---

## 十三、用户习惯与偏好

- 柒木习惯用**中文**交流，指令简短
- 微信 bot 配置：lark-cli（飞书官方 CLI），详见 `~/.workbuddy/MEMORY.md`
- Obsidian 唯一正式库：`D:\Obsidian_品牌大脑_正式库`
- 禁止使用旧副本：`D:\安装程序的地方\下载软件\Obsidian_品牌大脑_极简启动版`
- 后台任务通知规则：禁止说"X分钟后通知你"但不建立实际通知机制
- 等待结果时禁止用 `run_in_background`，必须前台执行

---

*本文件写于 2026-08-26 14:05，由 WorkBuddy 会话编写。以本文件为准，旧交接文件 `交接_新窗口继续.md` 部分信息已过时。*

---

## 十四、SKU 活动选项 + 支付弹窗 + 详情页发货内容（2026-08-26 完成）

### 完成内容（三项全部落地并验证通过）

**1. SKU / 活动选项（前后端全链路）**
- 数据模型：产品新增 `skus[]`，每个 SKU 含 `id/name/price/stock/image/bundleItems`（独立价格、库存、发货明细）
- 商家后台 `public/admin.html`：产品编辑弹窗新增「SKU / 活动选项」管理区，可增删改、每项独立设名称/价格/库存/发货明细
- 商城首页 `views/home.html`：有 SKU 的产品显示「¥XX起」+「选择规格」按钮，底部弹窗选规格（含数量、发货内容预览、立即购买/加入购物车）
- 商品详情页 `views/product.html`：同样弹窗选规格；购物车键 `产品id#SKUid`
- 服务端 `server.js`：`POST /api/orders` 按 SKU 动态算名称/价格/图片/发货明细；`POST /api/products` 序列化清洗 skus；**双保险兼容**——`{id,skuId}` 分离字段 和 `id#skuId` 整体键 都能解析
- 订单页 `views/order.html`：商品明细与订单卡片均展示发货内容（📦）

**2. 微信支付弹窗排版（Task #6）**
- 微信内点「保存图片」不再弹 alert 挤成一团，改为专属步骤面板：标题「操作步骤」居中，1/2/3/4 每步左对齐、整行排开、步间空行，底部「返回收款码」按钮

**3. 详情页「该活动链接实际包含」按 SKU 动态显示（Task #4）**
- 有 SKU 的产品：隐藏固定区块，发货内容在 SKU 选择弹窗内随所选规格动态显示（优先 SKU 自身发货明细，无则回退产品级）

### 端到端验证结果
- 测试产品 3 个 SKU（¥199/¥499/¥799）创建成功、首页/详情页渲染正确
- SKU 下单：价 ¥499 正确覆盖产品价、名称自动拼接「测试产品·SKU验证 · 三瓶装」、发货明细用 SKU 自己的
- 旧格式 `id#SKUid` 整体传入兼容通过
- 测试数据已全部清理（云端订单 7 条、产品 6 个，本地订单 21 条、产品 6 个，零残留）

### ⚠️ 重要环境备注（本次发现）
- **网络抖动**：本机 Supabase 直连偶发失败（TLS 被 Clash 代理 127.0.0.1:7897 重置，读取大多成功、写入偶失败）。遇到「fetch failed」不要立刻判断代码错，**加重试机制重跑**即可。curl 不受影响（HTTP 201 正常）
- 服务当前以 `node server.js` 运行于 4100 端口（本次重启加载新代码）
- 云端 Render（buchu-shop.onrender.com）webhook 已断开，不会自动部署——如需上线本次改动，需手动推 GitHub + Render 手动 Deploy
